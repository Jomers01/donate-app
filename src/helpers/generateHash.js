// const argon2 = require('argon2');

function generateHash(password) {
  // const myHash = argon2.hash(password)
  // .then(hash => hash)
  // .catch(err => { console.log(`Un error interno ha ocurrido (${err})`) });

  // return myHash;
  return password;
}

export default generateHash;
